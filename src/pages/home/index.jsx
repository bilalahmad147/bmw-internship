import React from "react";
import Header from "../../components/header";
import DataGrid from "../../components/dataGrid";
const Home = (props) => {
  return (
    <div>
      <Header />
      <DataGrid />
    </div>
  );
};

export default Home;
